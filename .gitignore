# Build output
dist/
build/
*.tsbuildinfo

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Dependencies
# (backend/node_modules is already tracked in this repo; this only stops
#  *new* dependency trees from being committed.)
node_modules/

# Environment / secrets
.env
.env.local
*.local

# Editor / OS
.DS_Store
.idea/
.vscode/

# Demo-video pipeline scratch (raw recording, intermediates, logs).
# The narration clips in tools/demo-video/narration/ ARE committed on purpose:
# they are the offline fallback voice for the one-click render.
tools/demo-video/work/
tools/demo-video/.extras/

# Generated demo video — master render stays local (large);
# the 720p cut, subtitles and stills in docs/video/ ARE committed
docs/video/Dakshin-Marg-demo.mp4
docs/video/Dakshin-Marg-narration.mp3
docs/video/audio/
